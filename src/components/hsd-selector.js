import React from 'react';

const HsdSelector = () => {
    return (
        <div>
            <button>{'ROT'}</button>
            <button>{'RUT'}</button>
        </div>
    )
}

export default HsdSelector;